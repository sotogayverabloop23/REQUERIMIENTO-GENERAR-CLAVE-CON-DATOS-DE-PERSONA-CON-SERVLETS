<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Sonic</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');

    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #0057b7, #ffd200);
      font-family: 'Press Start 2P', cursive, monospace;
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    form {
      background-color: #003580cc;
      width: 420px;
      padding: 3rem 2.5rem;
      border-radius: 20px;
      box-shadow: 0 0 20px #ffd200aa;
      display: flex;
      flex-direction: column;
      align-items: stretch;
      border: 3px solid #ffd200;
    }

    label {
      font-weight: 700;
      font-size: 1rem;
      margin-top: 1rem;
      letter-spacing: 0.1em;
    }

    input[type="text"],
    input[type="date"] {
      margin-top: 0.5rem;
      padding: 0.8rem 1rem;
      font-family: 'Press Start 2P', monospace;
      font-size: 1rem;
      border-radius: 10px;
      border: none;
      background-color: #007bffcc;
      color: white;
      font-weight: 700;
      box-shadow: inset 0 0 12px #003580bb;
      transition: background-color 0.3s ease;
    }

    input[type="text"]:focus,
    input[type="date"]:focus {
      background-color: #0057b7;
      outline: none;
      box-shadow: 0 0 8px 3px #ffd200aa;
      color: #ffd200;
    }

    button {
      margin-top: 2.5rem;
      font-family: 'Press Start 2P', monospace;
      font-weight: 900;
      font-size: 1.1rem;
      background-color: #ffd200;
      color: #003580;
      padding: 1rem 0;
      border-radius: 15px;
      border: none;
      cursor: pointer;
      letter-spacing: 0.15em;
      box-shadow: 0 0 15px #ffd200dd;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    button:hover {
      background-color: #ffc400;
      color: #002a5c;
      box-shadow: 0 0 25px #ffeb3bdd;
    }
  </style>
</head>
<body>
	<form th:action="@{/feti}" method="post" autocomplete="off">
	  <label for="nombre">Nombre</label>
	  <input type="text" id="nombre" name="nombre" placeholder="Ingresa tu nombre" required />

	  <label for="apellido">Apellido</label>
	  <input type="text" id="apellido" name="apellido" placeholder="Ingresa tu apellido" required />

	  <label for="fechaNacimiento">Fecha de Nacimiento</label>
	  <input type="date" id="fechaNacimiento" name="fechaNacimiento" required />

	  <button type="submit">ENVIAR</button>
	</form>
</body>
</html>
